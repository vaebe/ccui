import nav from './nav';
import head from './head'
import markdown from './markdown';

const config = {
  title: 'Vue OkUI',
  description: ' Vue OkUI 组件库',
  head,
  markdown,
  themeConfig: {
    nav,
    logo: '/img/logo.svg'
  }
};

export default config
